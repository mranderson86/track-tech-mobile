import React from 'react';
import { View } from 'react-native';

function Separator() {
  return (
    <View
      style={{
        height: 0.5,
        width: '100%',
        backgroundColor: '#000'
      }}
    />
  );
}

export default Separator;
