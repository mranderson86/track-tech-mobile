import React from 'react';
import { View, Text } from 'react-native';

class Card extends React.Component {
  render() {
    return (
      <View>
        <Text>Card Item</Text>
      </View>
    );
  }
}

export default Card;
